{
    "name": "@skillpack/api",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "src/index.js",
    "dependencies": {
        "@skillpack/crypto": "workspace:*",
        "@skillpack/core": "workspace:*",
        "hono": "^4.8.2"
    },
    "devDependencies": {
        "wrangler": "^4.13.2"
    },
    "scripts": {
        "test": "bun test",
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "d1:migrate:local": "wrangler d1 migrations apply skillpack-db --local",
        "d1:migrate:remote": "wrangler d1 migrations apply skillpack-db"
    }
}
