// AUTO-GENERATED by scripts/build-dashboard.js — do not edit
export default "// apps/dashboard/src/ui/api.js\nfunction createApi({ baseUrl = \"/api\", getToken } = {}) {\n  async function call(path, { method = \"GET\", body, headers = {} } = {}) {\n    const init = { method, headers: { ...headers } };\n    if (getToken) {\n      const token = await getToken();\n      init.headers[\"authorization\"] = \"Bearer \" + token;\n    }\n    if (body !== undefined) {\n      init.headers[\"content-type\"] = \"application/json\";\n      init.body = JSON.stringify(body);\n    }\n    const response = await fetch(`${baseUrl}${path}`, init);\n    const text = await response.text();\n    return {\n      status: response.status,\n      ok: response.ok,\n      body: text ? safeJson(text) : null\n    };\n  }\n  return { call };\n}\nfunction safeJson(text) {\n  try {\n    return JSON.parse(text);\n  } catch {\n    return text;\n  }\n}\n\n// apps/dashboard/src/ui/formatters.js\nfunction formString(formData, key) {\n  return String(formData.get(key) || \"\").trim();\n}\nfunction optionalFormString(formData, key) {\n  const value = formString(formData, key);\n  return value.length > 0 ? value : undefined;\n}\nfunction formNumber(formData, key, fallback = 0) {\n  const value = Number(formData.get(key));\n  return Number.isFinite(value) ? value : fallback;\n}\nfunction optionalFormNumber(formData, key) {\n  const raw = formString(formData, key);\n  if (!raw)\n    return;\n  const value = Number(raw);\n  return Number.isFinite(value) ? value : undefined;\n}\nfunction toEpochSec(value) {\n  const ms = Date.parse(value);\n  return Number.isFinite(ms) ? Math.floor(ms / 1000) : null;\n}\nfunction toLocalValue(ms) {\n  const date = new Date(ms);\n  const yyyy = date.getFullYear();\n  const mm = String(date.getMonth() + 1).padStart(2, \"0\");\n  const dd = String(date.getDate()).padStart(2, \"0\");\n  const hh = String(date.getHours()).padStart(2, \"0\");\n  const min = String(date.getMinutes()).padStart(2, \"0\");\n  return yyyy + \"-\" + mm + \"-\" + dd + \"T\" + hh + \":\" + min;\n}\nfunction dateRangeLabel(startSec, endSec) {\n  if (!startSec || !endSec)\n    return \"unknown period\";\n  return new Date(startSec * 1000).toLocaleDateString() + \" - \" + new Date(endSec * 1000).toLocaleDateString();\n}\nfunction formatMoneyCents(cents, currency = \"USD\") {\n  const amount = Number(cents || 0) / 100;\n  return currency + \" \" + amount.toFixed(2);\n}\nfunction escapeHtml(value) {\n  return String(value ?? \"\").replaceAll(\"&\", \"&amp;\").replaceAll(\"<\", \"&lt;\").replaceAll(\">\", \"&gt;\").replaceAll('\"', \"&quot;\").replaceAll(\"'\", \"&#39;\");\n}\nfunction createOutput(el) {\n  return {\n    set(value, isError = false) {\n      el.textContent = typeof value === \"string\" ? value : JSON.stringify(value, null, 2);\n      el.classList.toggle(\"is-error\", isError);\n    }\n  };\n}\nfunction syncSelect(node, items, valueKey, label, placeholder) {\n  if (!node)\n    return;\n  const previous = node.value;\n  const options = [];\n  if (placeholder) {\n    options.push('<option value=\"\">' + escapeHtml(placeholder) + \"</option>\");\n  }\n  for (const item of items) {\n    options.push('<option value=\"' + escapeHtml(item[valueKey]) + '\">' + escapeHtml(label(item)) + \"</option>\");\n  }\n  node.innerHTML = options.join(\"\");\n  if (items.some((item) => item[valueKey] === previous)) {\n    node.value = previous;\n  } else if (items[0]) {\n    node.value = items[0][valueKey];\n  }\n}\nfunction syncOptionalSelect(node, items, valueKey, label, placeholder) {\n  if (!node)\n    return;\n  const previous = node.value;\n  const options = [\n    '<option value=\"\">' + escapeHtml(placeholder) + \"</option>\"\n  ];\n  for (const item of items) {\n    options.push('<option value=\"' + escapeHtml(item[valueKey]) + '\">' + escapeHtml(label(item)) + \"</option>\");\n  }\n  node.innerHTML = options.join(\"\");\n  node.value = items.some((item) => item[valueKey] === previous) ? previous : \"\";\n}\nfunction bindAsyncForm(el, handler, output) {\n  el.addEventListener(\"submit\", (event) => {\n    handler(event).catch((error) => {\n      output.set({ error: error.message }, true);\n    });\n  });\n}\n\n// apps/dashboard/src/ui/render/policy.js\nfunction mountPolicy(root, { api, state, onBillingSelectsRefresh, onSetMetrics }) {\n  const $ = (sel) => root.querySelector(sel);\n  const out = createOutput($(\"#policy-output\"));\n  function refreshWorkspaceCustomerOptions() {\n    const providerId = $(\"#workspace-provider\").value;\n    const customers = state.customers.filter((c) => !providerId || c.providerId === providerId);\n    syncSelect($(\"#workspace-customer\"), customers, \"customerId\", (c) => c.customerId, \"Create customer first\");\n  }\n  function renderHierarchy() {\n    $(\"#providers-list\").innerHTML = state.providers.length ? state.providers.map((p) => \"<li><strong>\" + escapeHtml(p.providerId) + \"</strong><span>\" + escapeHtml(p.name || \"Unnamed provider\") + \"</span></li>\").join(\"\") : \"<li>No providers yet.</li>\";\n    $(\"#customers-list\").innerHTML = state.customers.length ? state.customers.map((c) => \"<li><strong>\" + escapeHtml(c.customerId) + \"</strong><span>\" + escapeHtml(c.providerId + (c.name ? \" · \" + c.name : \"\")) + \"</span></li>\").join(\"\") : \"<li>No customers yet.</li>\";\n    $(\"#workspaces-list\").innerHTML = state.workspaces.length ? state.workspaces.map((w) => \"<li><strong>\" + escapeHtml(w.workspaceId) + \"</strong><span>\" + escapeHtml(w.providerId + \" / \" + w.customerId + \" / \" + w.status) + \"</span></li>\").join(\"\") : \"<li>No workspaces yet.</li>\";\n    syncSelect($(\"#customer-provider\"), state.providers, \"providerId\", (p) => p.providerId, \"Create provider first\");\n    syncSelect($(\"#workspace-provider\"), state.providers, \"providerId\", (p) => p.providerId, \"Create provider first\");\n    refreshWorkspaceCustomerOptions();\n    onBillingSelectsRefresh?.();\n    onSetMetrics?.();\n  }\n  async function refreshHierarchy() {\n    const providers = await api(\"/v1/providers\");\n    state.providers = providers.providers || [];\n    const customerPages = await Promise.all(state.providers.map((p) => api(\"/v1/providers/\" + encodeURIComponent(p.providerId) + \"/customers\")));\n    state.customers = customerPages.flatMap((r) => r.customers || []);\n    const workspaces = await api(\"/v1/workspaces\");\n    state.workspaces = workspaces.workspaces || [];\n    renderHierarchy();\n  }\n  async function handleProviderSubmit(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const res = await api(\"/v1/providers\", {\n      method: \"POST\",\n      body: {\n        providerId: String(fd.get(\"providerId\") || \"\").trim(),\n        name: String(fd.get(\"name\") || \"\").trim() || undefined\n      }\n    });\n    out.set(res);\n    event.currentTarget.reset();\n    await refreshHierarchy();\n  }\n  async function handleCustomerSubmit(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const providerId = String(fd.get(\"providerId\") || \"\").trim();\n    const res = await api(\"/v1/providers/\" + encodeURIComponent(providerId) + \"/customers\", {\n      method: \"POST\",\n      body: {\n        customerId: String(fd.get(\"customerId\") || \"\").trim(),\n        name: String(fd.get(\"name\") || \"\").trim() || undefined\n      }\n    });\n    out.set(res);\n    event.currentTarget.reset();\n    await refreshHierarchy();\n  }\n  async function handleWorkspaceSubmit(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const res = await api(\"/v1/workspaces\", {\n      method: \"POST\",\n      body: {\n        workspaceId: String(fd.get(\"workspaceId\") || \"\").trim(),\n        providerId: String(fd.get(\"providerId\") || \"\").trim(),\n        customerId: String(fd.get(\"customerId\") || \"\").trim(),\n        name: String(fd.get(\"name\") || \"\").trim() || undefined,\n        status: String(fd.get(\"status\") || \"\").trim() || undefined\n      }\n    });\n    out.set(res);\n    event.currentTarget.reset();\n    await refreshHierarchy();\n  }\n  async function handlePolicyIssue(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const seatId = String(fd.get(\"seatId\") || \"\").trim();\n    const toolName = String(fd.get(\"toolName\") || \"\").trim();\n    const startsAtSec = toEpochSec(String(fd.get(\"startsAt\") || \"\"));\n    const expiresAtSec = toEpochSec(String(fd.get(\"expiresAt\") || \"\"));\n    const graceUntilSec = toEpochSec(String(fd.get(\"graceUntil\") || \"\"));\n    const res = await api(\"/v1/policies/issue\", {\n      method: \"POST\",\n      body: {\n        policy: {\n          policyVersion: 1,\n          policyId: String(fd.get(\"policyId\") || \"\").trim(),\n          workspaceId: String(fd.get(\"workspaceId\") || \"\").trim(),\n          workspacePolicy: { mode: \"ENABLED\" },\n          seatPolicy: {\n            defaultMode: \"ENABLED\",\n            seats: { [seatId]: { mode: \"ENABLED\" } }\n          },\n          usagePolicy: {\n            unit: \"tool_call\",\n            thresholds: {\n              warningPct: Number(fd.get(\"warningPct\") || 100),\n              hardStopPct: Number(fd.get(\"hardStopPct\") || 120)\n            },\n            toolBudgets: { [toolName]: Number(fd.get(\"toolBudget\") || 100) }\n          },\n          timePolicy: {\n            workspace: { startsAtSec, expiresAtSec, graceUntilSec },\n            seatOverrides: {\n              [seatId]: { startsAtSec, expiresAtSec, graceUntilSec }\n            }\n          }\n        }\n      }\n    });\n    out.set(res);\n  }\n  async function handlePolicySync(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const res = await api(\"/v1/policies/sync\", {\n      method: \"POST\",\n      body: {\n        workspaceId: String(fd.get(\"workspaceId\") || \"\").trim(),\n        policyId: String(fd.get(\"policyId\") || \"\").trim() || undefined\n      }\n    });\n    out.set(res);\n  }\n  bindAsyncForm($(\"#provider-form\"), handleProviderSubmit, out);\n  bindAsyncForm($(\"#customer-form\"), handleCustomerSubmit, out);\n  bindAsyncForm($(\"#workspace-form\"), handleWorkspaceSubmit, out);\n  bindAsyncForm($(\"#policy-issue-form\"), handlePolicyIssue, out);\n  bindAsyncForm($(\"#policy-sync-form\"), handlePolicySync, out);\n  $(\"#refresh-hierarchy\").addEventListener(\"click\", () => {\n    refreshHierarchy().catch((e) => out.set({ error: e.message }, true));\n  });\n  $(\"#workspace-provider\").addEventListener(\"change\", refreshWorkspaceCustomerOptions);\n  return { refreshHierarchy };\n}\n\n// apps/dashboard/src/ui/render/usage.js\nfunction mountUsage(root, { api, state, onSetMetrics }) {\n  const $ = (sel) => root.querySelector(sel);\n  const out = createOutput($(\"#usage-output\"));\n  function renderUsage() {\n    $(\"#usage-body\").innerHTML = state.usage.length ? state.usage.map((row) => \"<tr><td>\" + escapeHtml(row.providerId) + \"</td><td>\" + escapeHtml(row.customerId) + \"</td><td>\" + escapeHtml(row.workspaceId) + \"</td><td>\" + escapeHtml(row.seatId) + \"</td><td>\" + escapeHtml(row.skillId || \"—\") + \"</td><td>\" + escapeHtml(row.tool) + \"</td><td>\" + escapeHtml(String(row.totalCalls)) + \"</td></tr>\").join(\"\") : '<tr><td colspan=\"7\">No usage rows.</td></tr>';\n    onSetMetrics?.();\n  }\n  async function refreshUsage(form = $(\"#usage-filter-form\")) {\n    const params = new URLSearchParams;\n    const fd = new FormData(form);\n    for (const [key, value] of fd.entries()) {\n      if (typeof value === \"string\" && value.trim().length > 0) {\n        params.set(key, value.trim());\n      }\n    }\n    const res = await api(\"/v1/usage/summary\" + (params.size ? \"?\" + params.toString() : \"\"));\n    state.usage = res.summary || [];\n    renderUsage();\n    out.set(res);\n  }\n  async function handleMeterUpload(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const file = fd.get(\"file\");\n    if (!(file instanceof File))\n      throw new Error(\"missing_meter_file\");\n    const text = await file.text();\n    const events = text.split(/\\r?\\n/).map((line) => line.trim()).filter(Boolean).map((line, index) => {\n      try {\n        return JSON.parse(line);\n      } catch {\n        throw new Error(\"invalid_jsonl_line_\" + (index + 1));\n      }\n    });\n    const context = {};\n    for (const key of [\n      \"providerId\",\n      \"customerId\",\n      \"seatId\",\n      \"skillId\",\n      \"bundleId\",\n      \"leaseJti\"\n    ]) {\n      const value = String(fd.get(key) || \"\").trim();\n      if (value)\n        context[key] = value;\n    }\n    const res = await api(\"/v1/meter/upload\", {\n      method: \"POST\",\n      body: {\n        workspaceId: String(fd.get(\"workspaceId\") || \"\").trim(),\n        context,\n        events\n      }\n    });\n    out.set(res);\n    await refreshUsage();\n  }\n  bindAsyncForm($(\"#meter-upload-form\"), handleMeterUpload, out);\n  $(\"#refresh-usage\").addEventListener(\"click\", () => {\n    refreshUsage().catch((e) => out.set({ error: e.message }, true));\n  });\n  $(\"#usage-filter-form\").addEventListener(\"submit\", (event) => {\n    event.preventDefault();\n    refreshUsage(event.currentTarget).catch((e) => out.set({ error: e.message }, true));\n  });\n  return { refreshUsage };\n}\n\n// apps/dashboard/src/ui/render/billing.js\nfunction mountBilling(root, { api, state, onSetMetrics }) {\n  const $ = (sel) => root.querySelector(sel);\n  const out = createOutput($(\"#billing-output\"));\n  const E = escapeHtml;\n  function refreshBillingCustomerOptions(prefix) {\n    const providerId = $(\"#billing-\" + prefix + \"-provider\").value;\n    const customers = state.customers.filter((c) => !providerId || c.providerId === providerId);\n    const sync = prefix === \"rule\" ? syncOptionalSelect : syncSelect;\n    sync($(\"#billing-\" + prefix + \"-customer\"), customers, \"customerId\", (c) => c.customerId, prefix === \"rule\" ? \"Any customer\" : \"Create customer first\");\n  }\n  function refreshBillingWorkspaceOptions(prefix) {\n    const providerId = $(\"#billing-\" + prefix + \"-provider\").value;\n    const customerId = $(\"#billing-\" + prefix + \"-customer\").value;\n    const workspaces = state.workspaces.filter((w) => (!providerId || w.providerId === providerId) && (!customerId || w.customerId === customerId));\n    syncOptionalSelect($(\"#billing-\" + prefix + \"-workspace\"), workspaces, \"workspaceId\", (w) => w.workspaceId, \"Any workspace\");\n  }\n  function refreshBillingSelectOptions() {\n    for (const prefix of [\"rule\", \"invoice\"]) {\n      syncSelect($(\"#billing-\" + prefix + \"-provider\"), state.providers, \"providerId\", (p) => p.providerId, \"Create provider first\");\n      refreshBillingCustomerOptions(prefix);\n      refreshBillingWorkspaceOptions(prefix);\n    }\n    syncSelect($(\"#billing-handoff-invoice\"), state.invoices, \"invoiceId\", (inv) => inv.invoiceId + \" · \" + formatMoneyCents(inv.totalAmountCents, inv.currency), \"Draft invoice first\");\n  }\n  function renderBilling() {\n    const FM = formatMoneyCents;\n    $(\"#pricing-rules-list\").innerHTML = state.pricingRules.length ? state.pricingRules.map((r) => `<li><strong>${E(r.pricingRuleId)}</strong><span>${E(r.providerId + \" / \" + (r.customerId || \"any customer\") + \" / \" + (r.tool || \"any tool\") + \" / \" + FM(r.unitAmountCents, r.currency))}</span></li>`).join(\"\") : \"<li>No pricing rules yet.</li>\";\n    $(\"#invoices-list\").innerHTML = state.invoices.length ? state.invoices.map((inv) => `<li><strong>${E(inv.invoiceId)}</strong><span>${E(inv.customerId + \" / \" + inv.status + \" / \" + FM(inv.totalAmountCents, inv.currency))}</span></li>`).join(\"\") : \"<li>No draft invoices yet.</li>\";\n    $(\"#handoffs-list\").innerHTML = state.paymentHandoffs.length ? state.paymentHandoffs.slice(-5).reverse().map((h) => `<li><strong>${E(h.provider)}</strong><span>${E(h.invoiceId + \" / \" + h.status + (h.checkoutUrl ? \" / \" + h.checkoutUrl : \"\"))}</span></li>`).join(\"\") : \"<li>No handoffs created in this session.</li>\";\n    $(\"#invoices-body\").innerHTML = state.invoices.length ? state.invoices.map((inv) => `<tr><td>${E(inv.invoiceId)}</td><td>${E(inv.customerId)}</td><td>${E(inv.status)}</td><td>${E(dateRangeLabel(inv.periodStartSec, inv.periodEndSec))}</td><td>${E(FM(inv.totalAmountCents, inv.currency))}</td></tr>`).join(\"\") : '<tr><td colspan=\"5\">No invoices drafted yet.</td></tr>';\n    refreshBillingSelectOptions();\n    onSetMetrics?.();\n  }\n  async function refreshBilling() {\n    const pr = await api(\"/v1/billing/pricing-rules\");\n    state.pricingRules = pr.pricingRules || [];\n    const inv = await api(\"/v1/billing/invoices\");\n    state.invoices = inv.invoices || [];\n    renderBilling();\n    out.set({ pricingRules: state.pricingRules.length, invoices: state.invoices.length });\n  }\n  async function handleBillingPricingRuleSubmit(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const paymentProvider = {\n      provider: formString(fd, \"paymentProvider\") || \"manual\",\n      productId: optionalFormString(fd, \"productId\"),\n      priceId: optionalFormString(fd, \"priceId\")\n    };\n    const res = await api(\"/v1/billing/pricing-rules\", {\n      method: \"POST\",\n      body: {\n        pricingRuleId: formString(fd, \"pricingRuleId\"),\n        providerId: formString(fd, \"providerId\"),\n        customerId: optionalFormString(fd, \"customerId\"),\n        workspaceId: optionalFormString(fd, \"workspaceId\"),\n        skillId: optionalFormString(fd, \"skillId\"),\n        bundleId: optionalFormString(fd, \"bundleId\"),\n        tool: optionalFormString(fd, \"tool\"),\n        currency: formString(fd, \"currency\") || \"USD\",\n        unitAmountCents: formNumber(fd, \"unitAmountCents\"),\n        includedUnits: optionalFormNumber(fd, \"includedUnits\"),\n        minimumAmountCents: optionalFormNumber(fd, \"minimumAmountCents\"),\n        paymentProvider\n      }\n    });\n    out.set(res);\n    event.currentTarget.reset();\n    event.currentTarget.elements.currency.value = \"USD\";\n    event.currentTarget.elements.unitAmountCents.value = \"25\";\n    event.currentTarget.elements.includedUnits.value = \"0\";\n    event.currentTarget.elements.minimumAmountCents.value = \"0\";\n    await refreshBilling();\n  }\n  async function handleBillingInvoiceDraft(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const res = await api(\"/v1/billing/invoices/draft\", {\n      method: \"POST\",\n      body: {\n        invoiceId: optionalFormString(fd, \"invoiceId\"),\n        providerId: formString(fd, \"providerId\"),\n        customerId: formString(fd, \"customerId\"),\n        workspaceId: optionalFormString(fd, \"workspaceId\"),\n        periodStartSec: toEpochSec(formString(fd, \"periodStart\")),\n        periodEndSec: toEpochSec(formString(fd, \"periodEnd\")),\n        currency: optionalFormString(fd, \"currency\")\n      }\n    });\n    out.set(res);\n    await refreshBilling();\n  }\n  async function handleBillingPaymentHandoff(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const invoiceId = formString(fd, \"invoiceId\");\n    const customer = optionalFormString(fd, \"customerEmail\") || optionalFormString(fd, \"customerName\") ? {\n      email: optionalFormString(fd, \"customerEmail\"),\n      name: optionalFormString(fd, \"customerName\")\n    } : undefined;\n    const res = await api(\"/v1/billing/invoices/\" + encodeURIComponent(invoiceId) + \"/payment-handoff\", {\n      method: \"POST\",\n      body: {\n        provider: formString(fd, \"provider\") || \"manual\",\n        returnUrl: optionalFormString(fd, \"returnUrl\"),\n        customer\n      }\n    });\n    if (res.paymentHandoff)\n      state.paymentHandoffs.push(res.paymentHandoff);\n    renderBilling();\n    out.set(res);\n  }\n  bindAsyncForm($(\"#billing-pricing-rule-form\"), handleBillingPricingRuleSubmit, out);\n  bindAsyncForm($(\"#billing-invoice-draft-form\"), handleBillingInvoiceDraft, out);\n  bindAsyncForm($(\"#billing-payment-handoff-form\"), handleBillingPaymentHandoff, out);\n  $(\"#billing-rule-provider\").addEventListener(\"change\", () => {\n    refreshBillingCustomerOptions(\"rule\");\n    refreshBillingWorkspaceOptions(\"rule\");\n  });\n  $(\"#billing-rule-customer\").addEventListener(\"change\", () => refreshBillingWorkspaceOptions(\"rule\"));\n  $(\"#billing-invoice-provider\").addEventListener(\"change\", () => {\n    refreshBillingCustomerOptions(\"invoice\");\n    refreshBillingWorkspaceOptions(\"invoice\");\n  });\n  $(\"#billing-invoice-customer\").addEventListener(\"change\", () => refreshBillingWorkspaceOptions(\"invoice\"));\n  $(\"#refresh-billing\").addEventListener(\"click\", () => {\n    refreshBilling().catch((e) => out.set({ error: e.message }, true));\n  });\n  return { refreshBilling, refreshBillingSelectOptions };\n}\n\n// apps/dashboard/src/ui/render/tsa.js\nfunction mountTsa(root, { api, state }) {\n  const $ = (sel) => root.querySelector(sel);\n  const out = createOutput($(\"#tsa-output\"));\n  function renderAttestations() {\n    $(\"#attestations-body\").innerHTML = state.attestations.length ? state.attestations.map((row) => \"<tr><td>\" + escapeHtml(row.customerId) + \"</td><td>\" + escapeHtml(row.seatId || \"default\") + \"</td><td>\" + escapeHtml(row.operatorId) + \"</td><td>\" + escapeHtml(row.ticketId) + \"</td><td>\" + escapeHtml(new Date(row.recordedAtSec * 1000).toLocaleString()) + \"</td><td>\" + escapeHtml(row.reason) + \"</td></tr>\").join(\"\") : '<tr><td colspan=\"6\">No manual attestations.</td></tr>';\n  }\n  async function refreshAttestations(form = $(\"#attestation-filter-form\")) {\n    const params = new URLSearchParams;\n    const fd = new FormData(form);\n    for (const [key, value] of fd.entries()) {\n      if (typeof value === \"string\" && value.trim().length > 0) {\n        params.set(key, value.trim());\n      }\n    }\n    const res = await api(\"/v1/tsa/manual-attestations\" + (params.size ? \"?\" + params.toString() : \"\"));\n    state.attestations = res.records || [];\n    renderAttestations();\n    out.set(res);\n  }\n  async function handleTsaSubmit(event) {\n    event.preventDefault();\n    const fd = new FormData(event.currentTarget);\n    const res = await api(\"/v1/tsa/manual-attest\", {\n      method: \"POST\",\n      body: {\n        customerId: String(fd.get(\"customerId\") || \"\").trim(),\n        seatId: String(fd.get(\"seatId\") || \"\").trim() || \"default\",\n        operatorId: String(fd.get(\"operatorId\") || \"\").trim(),\n        ticketId: String(fd.get(\"ticketId\") || \"\").trim(),\n        reason: String(fd.get(\"reason\") || \"\").trim(),\n        attestedAtSec: toEpochSec(String(fd.get(\"attestedAt\") || \"\"))\n      }\n    });\n    out.set(res);\n    await refreshAttestations();\n  }\n  bindAsyncForm($(\"#tsa-form\"), handleTsaSubmit, out);\n  $(\"#refresh-attestations\").addEventListener(\"click\", () => {\n    refreshAttestations().catch((e) => out.set({ error: e.message }, true));\n  });\n  $(\"#attestation-filter-form\").addEventListener(\"submit\", (event) => {\n    event.preventDefault();\n    refreshAttestations(event.currentTarget).catch((e) => out.set({ error: e.message }, true));\n  });\n  return { refreshAttestations };\n}\n\n// apps/dashboard/src/ui/index.js\nvar state = {\n  config: null,\n  providers: [],\n  customers: [],\n  workspaces: [],\n  usage: [],\n  pricingRules: [],\n  invoices: [],\n  paymentHandoffs: [],\n  attestations: [],\n  clerkLoaded: false\n};\nfunction $(selector) {\n  return document.querySelector(selector);\n}\nfunction setMetrics() {\n  $(\"#metric-providers\").textContent = String(state.providers.length);\n  $(\"#metric-customers\").textContent = String(state.customers.length);\n  $(\"#metric-workspaces\").textContent = String(state.workspaces.length);\n  $(\"#metric-usage\").textContent = String(state.usage.length);\n  $(\"#metric-pricing-rules\").textContent = String(state.pricingRules.length);\n  $(\"#metric-invoices\").textContent = String(state.invoices.length);\n}\nfunction setDefaultTimes() {\n  const now = Date.now();\n  $(\"#policy-issue-form\").elements.startsAt.value = toLocalValue(now);\n  $(\"#policy-issue-form\").elements.expiresAt.value = toLocalValue(now + 24 * 60 * 60 * 1000);\n  $(\"#policy-issue-form\").elements.graceUntil.value = toLocalValue(now + 72 * 60 * 60 * 1000);\n  $(\"#tsa-form\").elements.attestedAt.value = toLocalValue(now);\n  $(\"#billing-invoice-draft-form\").elements.periodStart.value = toLocalValue(now - 30 * 24 * 60 * 60 * 1000);\n  $(\"#billing-invoice-draft-form\").elements.periodEnd.value = toLocalValue(now);\n}\nasync function loadConfig() {\n  const response = await fetch(\"/app-config\");\n  state.config = await response.json();\n  $(\"#config-summary\").textContent = \"Auth mode: \" + state.config.authMode + \" · Clerk host: \" + (state.config.clerkFrontendApiHost || \"missing\") + \" · Proxy base: \" + (state.config.apiProxyBase || \"missing\");\n}\nasync function loadScript(src, attributes = {}) {\n  await new Promise((resolve, reject) => {\n    const script = document.createElement(\"script\");\n    script.src = src;\n    script.defer = true;\n    script.crossOrigin = \"anonymous\";\n    for (const [key, value] of Object.entries(attributes)) {\n      script.setAttribute(key, value);\n    }\n    script.onload = () => resolve();\n    script.onerror = () => reject(new Error(\"script_load_failed\"));\n    document.head.appendChild(script);\n  });\n}\nasync function ensureClerk() {\n  if (state.clerkLoaded)\n    return window.Clerk;\n  if (!state.config?.clerkPublishableKey || !state.config?.clerkFrontendApiHost) {\n    throw new Error(\"clerk_not_configured\");\n  }\n  const host = state.config.clerkFrontendApiHost;\n  await loadScript(\"https://\" + host + \"/npm/@clerk/ui@1/dist/ui.browser.js\");\n  await loadScript(\"https://\" + host + \"/npm/@clerk/clerk-js@6/dist/clerk.browser.js\", { \"data-clerk-publishable-key\": state.config.clerkPublishableKey });\n  await window.Clerk.load({\n    ui: { ClerkUI: window.__internal_ClerkUICtor }\n  });\n  state.clerkLoaded = true;\n  return window.Clerk;\n}\nasync function getSessionToken() {\n  const clerk = await ensureClerk();\n  if (!clerk.session)\n    throw new Error(\"clerk_session_missing\");\n  const token = await clerk.session.getToken();\n  if (!token)\n    throw new Error(\"clerk_token_missing\");\n  return token;\n}\nfunction showAuthenticatedShell() {\n  $(\"#auth-root\").innerHTML = \"\";\n  $(\"#app-shell\").hidden = false;\n}\nfunction showLockedShell(message) {\n  $(\"#app-shell\").hidden = true;\n  $(\"#auth-root\").innerHTML = '<div class=\"output is-error\">' + escapeHtml(message) + \"</div>\";\n}\nasync function renderAuthState(refreshAll) {\n  const clerk = await ensureClerk();\n  const authRoot = $(\"#auth-root\");\n  const userRoot = $(\"#user-root\");\n  authRoot.innerHTML = \"\";\n  userRoot.innerHTML = \"\";\n  if (clerk.isSignedIn) {\n    showAuthenticatedShell();\n    const userButton = document.createElement(\"div\");\n    userRoot.appendChild(userButton);\n    clerk.mountUserButton(userButton);\n    await refreshAll();\n    return;\n  }\n  $(\"#app-shell\").hidden = true;\n  const signIn = document.createElement(\"div\");\n  authRoot.appendChild(signIn);\n  clerk.mountSignIn(signIn, {\n    signUpUrl: state.config.clerkSignUpUrl || undefined\n  });\n}\nasync function bootstrap() {\n  await loadConfig();\n  setDefaultTimes();\n  const rawApi = createApi({\n    baseUrl: state.config?.apiProxyBase ?? \"/api\",\n    getToken: getSessionToken\n  });\n  async function api(path, options) {\n    const { ok, body } = await rawApi.call(path, options);\n    if (!ok)\n      throw new Error(body?.error ?? \"request_failed\");\n    return body;\n  }\n  const { refreshBilling, refreshBillingSelectOptions } = mountBilling(document, { api, state, onSetMetrics: setMetrics });\n  const { refreshHierarchy } = mountPolicy(document, {\n    api,\n    state,\n    onBillingSelectsRefresh: refreshBillingSelectOptions,\n    onSetMetrics: setMetrics\n  });\n  const { refreshUsage } = mountUsage(document, {\n    api,\n    state,\n    onSetMetrics: setMetrics\n  });\n  const { refreshAttestations } = mountTsa(document, { api, state });\n  async function refreshAll() {\n    await refreshHierarchy();\n    await refreshUsage();\n    await refreshBilling();\n    await refreshAttestations();\n  }\n  try {\n    const clerk = await ensureClerk();\n    clerk.addListener(() => {\n      renderAuthState(refreshAll).catch((error) => {\n        showLockedShell(error.message);\n      });\n    });\n    await renderAuthState(refreshAll);\n  } catch (error) {\n    showLockedShell(error.message || \"clerk_init_failed\");\n  }\n}\nbootstrap().catch((error) => {\n  showLockedShell(error.message || \"dashboard_bootstrap_failed\");\n});\n";
