{
  "name": "skillpack-dashboard",
  "main": "src/index.js",
  "compatibility_date": "2026-04-22",

  // Public vars and secrets are resolved from
  // deploy/hosted-control-plane.manifest.json so the API/dashboard contract
  // lives in one place.
  "vars": {},
  "rules": [
    { "type": "Text", "globs": ["**/*.css"] }
  ]
}
